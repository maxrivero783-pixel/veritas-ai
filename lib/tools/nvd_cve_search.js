import { clamp,fetchJson,errorOutput,lines,truncate } from './_publicData.js';export async function run(a={},ctx={}){if(!a.cve&&!a.keyword)return {status:'error',output:'Missing cve or keyword'};const u=new URL('https://services.nvd.nist.gov/rest/json/cves/2.0');u.searchParams.set(a.cve?'cveId':'keywordSearch',a.cve||a.keyword);u.searchParams.set('resultsPerPage',clamp(a.limit,10,1,20));const h={Accept:'application/json'};if(ctx.env?.NVD_API_KEY)h.apiKey=ctx.env.NVD_API_KEY;const r=await fetchJson(u,{headers:h});if(!r.ok)return errorOutput('NVD',r);return {status:'ok',output:truncate(`NVD — ${a.cve||a.keyword}\n${lines(r.data?.vulnerabilities,(x,i)=>`#${i+1} ${x.cve.id}\n${x.cve.descriptions?.find(d=>d.lang==='en')?.value||''}`)||'Sin resultados'}`)}}export default {run};
