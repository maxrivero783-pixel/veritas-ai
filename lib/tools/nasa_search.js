import {clamp,fetchJson,errorOutput,lines,truncate} from './_publicData.js';export async function run(a={}){if(!a.query)return{status:'error',output:'Missing query'};const u=new URL('https://images-api.nasa.gov/search');u.searchParams.set('q',a.query);u.searchParams.set('media_type',a.media_type||'image');const r=await fetchJson(u);if(!r.ok)return errorOutput('NASA',r);const x=(r.data?.collection?.items||[]).slice(0,clamp(a.limit,10,1,20));return{status:'ok',output:truncate(`NASA — ${a.query}\n${lines(x,(v,i)=>{const d=v.data?.[0]||{};return `#${i+1} ${d.title||'Sin título'}\n${d.description||''}\n${v.links?.[0]?.href||''}`})||'Sin resultados'}`)}}export default{run};
